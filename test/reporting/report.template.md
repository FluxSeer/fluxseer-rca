# <Suite Name> Test Report

- Contract: `fluxseer-test-report/v1`
- Run ID: `<run-id>`
- Source commit: `<commit>`
- Environment: `<context / namespace / image or local runtime>`
- Result: `<PASS|FAIL>` (`<passed>/<total>`)

| Scenario | Result | Expected | Actual | Differences | Artifacts |
| --- | --- | --- | --- | --- | --- |
| `<scenario-id>` | PASS | `Completed/Confirmed`; provider=1 | `Completed/Confirmed`; provider=1 | none | `scenario.json` |

## Differences

List each failed scenario and the exact JSON path, expected value, and actual
value. Write `None` when all scenarios match.

## Aggregate Metrics

Include only suite-level measurements. Do not replace per-scenario output with
aggregate counts.
