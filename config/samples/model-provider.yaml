apiVersion: aiops.platform/v1alpha1
kind: ModelProvider
metadata:
  name: heuristic-provider
  namespace: fluxseer-rca-system
spec:
  provider: heuristic
  model: built-in
  timeout: 5s
  maxTokens: 512
