# FluxAgent v0.3.0-beta.1

Status: Published prerelease

Release type: Prerelease / Beta

Release version: `v0.3.0-beta.1`

Schema freeze baseline: `2821e254b65fc54bf6fa521aec89bf7c48240667`

Release candidate gate commit: `ab4f2bbbde95b99fe2336535e3c16c71a64ef1b9`

Release commit: `b55f57920eb4ebcef2e454c18ba9437081362287`

GitHub Release: <https://github.com/FluxSeer/FluxAgent/releases/tag/v0.3.0-beta.1>

Frozen API identity: `aiops.platform/v1alpha1`

## Status

`v0.3.0-beta.1` is the first FluxAgent beta built around a frozen RCA status contract.

Current publication state:

```text
v0.3 schema frozen:                    YES
v0.3 release candidate gate:           IMPLEMENTED
v0.3 release candidate verification:   PASSED
publication approval:                  COMPLETE
release published:                     YES
provenance verification:               PASSED
```

This release was published from the annotated `v0.3.0-beta.1` tag after tag-level release verification and registry provenance checks.

## Product Value

`v0.3.0-beta.1` turns FluxAgent from a read-only RCA workflow into a more auditable RCA control-plane contract.

Highlights:

- structured and auditable `InvestigationRequest.status`
- evidence-linked claims and independent verifier status
- durable provider execution checkpoints
- deterministic execution identity and finding identity
- incident occurrence and lineage tracking
- evidence provenance, digest, retention, and classification metadata
- datasource network safety and query policy boundaries
- hosted-provider data egress policy with explicit opt-in
- required evidence profiles and `NoIssueFound` gates
- raw-to-final replay coverage through deterministic fixtures
- low-cardinality RCA metrics and rule-pack validation

## Verified Gate

Verified on 2026-07-29 with:

```sh
make verify-release-v0.3-beta
```

Verified coverage:

```text
verify-release-inputs
verify-v0.3-schema-freeze
verify-v0.2-beta
verify-rule-packs
verify-rule-packs-kind
verify-e2e-kind
verify-investigation-kind
verify-artifact-identity
verify-packaging-consistency
verify-build-reproducibility
verify-lifecycle-kind
verify-release-cleanup
```

Observed result:

```text
PASS
```

The gate verified:

- frozen schema contract consistency
- Go tests and rendered Kustomize manifests
- Helm lint/render and rule pack rendering
- Kubernetes baseline, Prometheus baseline, and Loki baseline rule packs
- kind E2E `RiskRule -> RiskSignal -> RCA`
- `InvestigationRequest -> RCA` and optional `RiskSignal` promotion
- datasource degradation for missing datasource and capability mismatch
- hosted provider degradation for auth failure and rate limiting
- artifact identity for binary metadata and OCI labels
- chart `version` / `appVersion` / image tag consistency
- reproducible operator and demo image builds on `linux/amd64`
- Helm install, upgrade, uninstall, and CRD retention behavior
- kind cluster and release image cleanup hygiene

## Important Boundaries

This release keeps FluxAgent intentionally narrow:

- read-only applies to user workloads
- heuristic remains the no-secret default provider
- hosted-provider evidence transmission requires explicit `allowExternalTransmission=true`
- reasoning providers do not receive unrestricted cluster access
- confidence is a ranking score, not a calibrated probability
- `NoIssueFound` requires profile-specific evidence completeness
- provider calls are not exactly-once before durable checkpoint persistence
- bounded agentic investigation is not part of `v0.3.0-beta.1`
- remediation remains outside the core v0.3 RCA path

## Compatibility

Canonical RCA truth:

```text
InvestigationRequest.status
```

Compatibility projections:

```text
status.summary
status.hypothesis
status.confidence
status.provider
RiskSignal compatibility RCA fields
DirectRiskSignal path
```

The frozen API group is:

```yaml
apiVersion: aiops.platform/v1alpha1
```

Future API-group or resource renames require an explicit migration path and must not be treated as cosmetic changes.

## Publication Result

Published state:

```text
annotated tag created:        YES
tag target recorded:          YES
operator image published:     YES
demo image published:         YES
Helm OCI chart published:     YES
GitHub Release prerelease:    YES
published digests recorded:   YES
provenance verification:      PASSED
```

Provenance must verify:

```text
Git tag target
= binary embedded GitCommit
= OCI org.opencontainers.image.revision
= published image source revision
```

Version identity must verify:

```text
Binary Version
= OCI org.opencontainers.image.version
= Helm appVersion
= v0.3.0-beta.1
```

Helm chart identity:

```text
Chart version: 0.3.0-beta.1
App version:   v0.3.0-beta.1
```

## Artifact Digests

Immutable release references:

```text
Operator image:
  ghcr.io/fluxseer/fluxagent/operator:v0.3.0-beta.1
  digest: sha256:2573a6180957a2aaad0db10ab43d0b828b5381854a7426af8b6c66c20b30bafe

Demo observability image:
  ghcr.io/fluxseer/fluxagent/demo-observability:v0.3.0-beta.1
  digest: sha256:10357ad614d691804673b188bec93f3b5dc2e88873aa52985400f1a1922e804a

Helm chart:
  oci://ghcr.io/fluxseer/fluxagent/charts/fluxagent
  version: 0.3.0-beta.1
  digest: sha256:c8228c93ca7fd59ff7f99eac5adbbda125dfe0dfe7bb733b0e873a8f01b38b9d

GitHub Release assets:
  fluxagent-0.3.0-beta.1.tgz
  digest: sha256:b4df6d74cf74d57a1cc3e5933c3ffcbd4450196571776b6890b31d4e47a2cca3

Helm package checksum:
  SHA256SUMS asset digest: sha256:9958c7a92460f8054ebdf326068c0af3161ef2d96d41a26aab7f94117158ca05
```

## Post-Release Direction

After publication, the active development stage is `v0.3 Beta Stabilization and Evidence-Driven Optimization`.

`v0.3.x` should focus on correctness, security, schema-compatible provider and verifier fixes, fixture expansion, diagnostics, metrics, and installation experience. Work that requires new canonical schema fields, changed frozen enum semantics, changed execution identity, or expanded remediation behavior should be treated as a `v0.4` candidate or an explicit schema-freeze exception.
